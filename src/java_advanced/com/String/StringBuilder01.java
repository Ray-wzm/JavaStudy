/**
 * @author: 一只羊驼
 * @date: 2024/2/20
 */

package java_advanced.com.String;

public class StringBuilder01 {
    //一个可变的字符序列。提供一个与StringBuffer兼容得API，不保证同步（可能存在多线程问题）；
    //当字符串缓冲区被单个线程调用得时候，建议先采用该类
    //StringBuilder主要操作是append和insert方法，可以重载这些方法，可以接收任意类型的数据
    public static void main(String args[]) {
        StringBuilder stringBuilder = new StringBuilder();
    }
}
